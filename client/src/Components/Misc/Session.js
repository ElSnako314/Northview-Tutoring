import React from 'react'
import { Table } from 'reactstrap';

const Session = ({id, tutor, subject, date, timeframe, notes}) => {
  return (
    <div>
        <Table>
            <tbody>
                <tr>
                    <td>

                    </td>
                </tr>
            </tbody>
        </Table>
    </div>
  )
}

export default Session